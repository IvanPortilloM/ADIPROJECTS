# CONTEXTO — Refactor de acceso a datos (ADIGGM)

> Documento de handoff para continuar el refactor en una sesión nueva de Claude Code.
> Para retomar: pídele al agente **"Lee CONTEXTO-REFACTOR.md y continúa con la tarea inmediata"**.

## 0. Directiva de eficiencia (GASTAR POCOS TOKENS — prioridad)
- Responde **breve**, sin preámbulos ni repetir el plan; ve directo a la acción.
- Lee SOLO lo necesario: usa **Grep/Glob** para ubicar y **Read con offset/limit** en regiones puntuales. NO leas archivos completos ni los releas; NO re-leas un archivo que acabas de editar (el harness ya rastrea el estado).
- Ediciones **quirúrgicas** con Edit; NO reescribas archivos enteros ni pegues código grande en el chat (referencia `archivo:línea`).
- Build siempre con `/clp:ErrorsOnly` y `| Select-Object -Last 20` (no vuelques logs enormes). No repitas builds innecesarios.
- Trabaja **UN formulario/módulo por turno**: build verde → commit → y actualiza §6/§9/§13 de ESTE archivo. Así el contexto del chat se mantiene corto y el progreso queda fuera del chat.
- No abras chats largos: cuando el contexto crezca, deja todo committeado + este archivo actualizado y continúa en una sesión nueva.
- **En CADA migración de form: análisis de errores de lógica** (transacciones, catches que tragan errores, éxito incondicional, DBNull, estado UI). Documentar hallazgos estilo §14; los GRAVES se reparan en la misma migración (autorizado por el usuario 2026-06-10); latentes/diseño se documentan y consultan.
- **Trucos de ahorro probados**: (a) XSD: extraer CommandText+params con UN script PowerShell ([xml] + SelectNodes por FillMethodName/Name), NO con múltiples Read; (b) limpieza de Designer: las líneas repetitivas (campos, ClearBeforeFill, dgv.DataSource) se quitan en bloque con PowerShell Replace, no con N Edits; (c) si ADIGGM.exe está corriendo, compilar con `/p:OutputPath=bin\Debug-verify\` y borrar la carpeta después; (d) commit con `| Select-Object -First 2` para no volcar salida; (e) los archivos `*.tmp.<pid>.<hash>` que aparezcan son residuos de edición: borrarlos, no commitearlos (cuidado con `git add -A`, preferir add selectivo); (f) **§13.b en grids GRANDES**: en vez de leer ~200 líneas de Designer, volcar columnas en compacto con un script PowerShell (regex sobre `Columns.AddRange` para orden+membresía por grid, y por columna extraer type/DataPropertyName/HeaderText/Visible/Width/AutoSizeMode/ReadOnly). ⚠️ el script puede NO captar el `Format`/estilo cuando viene de una variable `dataGridViewCellStyleN` (verificar con `grep "DefaultCellStyle = dataGridViewCellStyle"`); y OJO que algún `cellStyle` sea del GRID (AlternatingRows/DefaultCellStyle) y NO de una columna → conservarlo. Para limpiar el Designer, borrar por RANGOS DE LÍNEA con PowerShell (HashSet de nº de línea sobre el archivo original, en UNA pasada), NO con regex greedy. **Mejor aún (forms con varios grids): `del_cols.ps1`** = borrado por PATRÓN anclado al nombre de columna (`this.<col>`, field decl, `// <col>`, miembros AddRange) + `del_match.ps1` para los cellStyle de COLUMNA huérfanos (`dataGridViewCellStyle<N>\b`, listando solo los N de columna y CONSERVANDO los del grid 1/10/21/30 = AlternatingRows). ⚠️⚠️ **CASE-SENSITIVE**: las columnas auto-generadas se llaman `dataGridViewTextBoxColumnN` con **d minúscula** (no `DataGridView...`); si pasas mal el case NO se borran y queda el `});` del AddRange sin su `{` → desbalance de llaves (open≠close) → "Token ; no válido en declaración de miembro". Verificar SIEMPRE tras borrar: `grep -c "{" vs "}"` balanceado y `grep "});"` = 0.

## 1. Proyecto
- WinForms **.NET Framework 4.6.2**, proyecto **NO-SDK** (packages.config). C# 7+ (hay tuplas/ValueTuple).
- Raíz: `C:\Users\jportillo\Dropbox\Desarrollo ADI (DEV)\ADIPROJECTS` | Solución: `ADIPROJECTS.sln`
- Git: `origin = https://github.com/IvanPortilloM/ADIPROJECTS` (rama `master`).
- Proyectos: **ADIGGM** (app principal), **WSCorreos** (servicio Windows, referencia a ADIGGM), **CheckBoxComboBox** (lib), **PRESUPUESTO**.
- ⚠️ `.git` está DENTRO de Dropbox → `git gc`/repack fallan ("Permission denied"). Ya está `git config gc.auto 0`. Recomendación: mover el repo fuera de Dropbox.
- Despliegue: ClickOnce a `\\ADIGGM\Publish\ADIPROJECTS\`.

## 2. Objetivo
1. Centralizar TODA cadena de conexión en App.config (un solo lugar).
2. Reemplazar los DataSets tipados por **Dapper** (v2.1.66, ya instalado) con patrón Repositorio.

## 3. Compilar / verificar (`dotnet build` NO funciona; usar MSBuild)
```powershell
$msbuild = "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\MSBuild\Current\Bin\MSBuild.exe"
& $msbuild "ADIGGM\ADIGGM.csproj" /t:Build /p:Configuration=Debug /p:Platform=AnyCPU /m /nologo /verbosity:minimal /clp:ErrorsOnly
```
- Validar build (exit 0) tras CADA cambio.
- csproj NO-SDK: CADA `.cs` nuevo DEBE agregarse a `<Compile Include="...">` en `ADIGGM\ADIGGM.csproj`.
- Arreglos habilitantes ya hechos: `CheckBoxComboBox.csproj` v4.0→v4.6.2; `<Reference Include="netstandard" />` en ADIGGM.csproj.
- NO se puede correr la app contra la BD real desde el agente: el USUARIO valida en ejecución (F5) cada form migrado.

## 4. Infraestructura ya creada
**`ADIGGM\CapaDatos\Conexion.cs`** = FUENTE ÚNICA. API: `Conexion.Cadena(nombre)`, `Conexion.CrearConexion(nombre)` [DbConnection agnóstico], `Conexion.CrearSql(nombre)` [SqlConnection]. Constantes: `Conexion.TRANSPORTE/.PERMISOS/.CA/.PRESUPUESTO/.COVIBASE/.COVIPRUEBAS/.SAC_MYSQL`. Compat: `Conexion.cn`, `Conexion.TransporteADI`.
**Respaldo por IP (996b640, para VPN sin DNS)**: appSettings `ServidorNombre`=ADIGGM / `ServidorIp`=192.168.2.77. `Conexion.Servidor` resuelve UNA vez por sesión (DNS del nombre → si falla, IP); `Cadena()` reescribe el host; `AjustarCadenasLegacy(Settings.Default)` (en Program.Main) cubre los DataSets tipados restantes; `AjustarRuta()` cubre rutas UNC (VarGlobales) y `urlReportes` se compone con `Conexion.Servidor`. Cadenas a otros hosts (MySQL adiggm.hn) NO se tocan. Al migrar forms NUEVOS no hay nada especial que hacer: todo pasa por Conexion.

**`ADIGGM\CapaDatos\RepositorioBase.cs`** (heredar con `: RepositorioBase`, ctor `: base(Conexion.XXX)`). Métodos protegidos:
- `Consultar<T>(sql,param) -> List<T>`; `PrimeroODefault<T>`; `Escalar<T>`; `Ejecutar(sql,param) -> int`
- `ConsultarTabla(sql,param) -> DataTable`; `CrearConexion() -> DbConnection` (transacciones manuales)
- `GuardarCambios(DataTable, insertSql, updateSql, deleteSql) -> int`: persiste por RowState (Added/Modified/Deleted) en transacción; **REEMPLAZA `TableAdapter.Update()`**. Los `@parámetros` deben llamarse IGUAL que las columnas; el PK identity NO va en el INSERT.

Repos (`ADIGGM\CapaDatos\`): RepositorioMotoristas, RepositorioPoliticas, RepositorioHistorialSalarios, RepositorioAsistencias, RepositorioReporteHoras, RepositorioTiposAsistencia, RepositorioFeriados, RepositorioInventario, RepositorioPermisos (Menu/SubMenu/DetSubMenu/permisos transaccionales), RepositorioUsuarios (TR_Usuarios p/combos, BD Transporte), RepositorioCA, RepositorioCodeas (dominio Codeas/SAC/covibase), RepositorioFAC (FAC_*), RepositorioTransporte (TR_* catálogo de TransporteAdiggm, creado 9a2ca14).
> **REGLA (usuario 2026-06-15)**: cada repositorio coherente con el DATO/BD que maneja, NO con el DataSet de origen. Un DataSet tipado puede tener tablas de OTRA base; ponerlas en el repo correcto por dominio (TR_*→Transporte, COD_/SAC→Codeas, FAC_*→FAC, IN_*→Inventario). Si un form cruza bases, dividir los métodos entre los repos correspondientes.
POCOs (`ADIGGM\CapaModelo\`): TipoAsistencia, DiaFeriado, MotoristaItem, HistorialSalario, TipoAsistenciaCombo, RegistroAsistenciaCab, TiempoTrabajado, ObservacionDia.
Dapper 2.1.66 en packages.config + csproj (DLL gitignored, se restaura con NuGet).

## 5. App.config — cadenas
Nombres canónicos (`ADIGGM\App.config <connectionStrings>`): `TransporteAdiggm`, `Permisos` (DB_Permisos), `CA`, `Presupuesto`, `Covibase`, `Covipruebas`, `SAC_MySql` (MySQL). Todas a `Data Source=ADIGGM` + `Encrypt=True;TrustServerCertificate=True`. Quedan entradas legacy `ADIGGM.Properties.Settings.*` que usan los DataSets tipados pendientes. `<appSettings>` con `TwilioAccountSid`/`TwilioAuthToken` VACÍOS.

## 6. Ya migrado (todo committeado y pusheado)
- **Conexión centralizada COMPLETA**: se eliminó `DbManager`; migradas cadenas inline de ~13 forms + `TranOrdenCompra` (tenía bug `.ToString()`) + WSCorreos. `ConfigurationManager.ConnectionStrings` solo vive en `Conexion.cs`.
- **Módulo HE migrado 100%** a Dapper (frmTiposAsistencia, frmFeriados, frmPoliticas, frmHistorialSalarios, frmCerrarPeriodo, frmCopiarAsistencia, frmReporteHorasExtras, frmAsistencias, frmEditarAsistencia).
- **Seguridad**: token Twilio PURGADO de todo el historial git (filter-repo + force-push).
- **DataSets — DsInventarioAdiggm ELIMINADO (c2831bf)**: frmTipoOp, frmBodegas, frmVisorExistencias, frmInventario migrados; combo bodegas de TranConfirmarOrden vía repo; `consultasInv` quitado de VarGlobales; .xsd/.cs/.xsc/.xss borrados y fuera del csproj. Primer DataSet retirado por completo. `RepositorioInventario` tiene: ListarTiposOperacion/GuardarTiposOperacion, ListarBodegas/GuardarBodegas, ListarVehiculosActivos, ListarCategoriasProductos, ListarProductosConTodos, ReporteExistencias (SP), ReporteProductosExistencia (SP). Patrón visor RDLC: BindingSource del Designer se conserva y en Load se le asigna el DataTable; maestro-detalle con DataSet plano + DataRelation (createConstraints:false por la fila "(TODOS)").

## 7. Patrón "Opción A" para DataSets tipados (mantenimiento: grid editable + TableAdapter.Update)
```csharp
// Repositorio (hereda RepositorioBase):
public DataTable ListarBodegas() => ConsultarTabla("SELECT IdBodega, NombreBodega, Activo FROM dbo.IN_Bodegas");
public int GuardarBodegas(DataTable t) => GuardarCambios(t,
   "INSERT INTO dbo.IN_Bodegas (NombreBodega, Activo) VALUES (@NombreBodega, @Activo)",   // PK identity NO va
   "UPDATE dbo.IN_Bodegas SET NombreBodega=@NombreBodega, Activo=@Activo WHERE IdBodega=@IdBodega",
   "DELETE FROM dbo.IN_Bodegas WHERE IdBodega=@IdBodega");
// Form .cs: campos `private DataTable _dt; private readonly RepositorioXxx _repo = new RepositorioXxx();`
//   Cargar(): _dt = _repo.ListarX(); bs.DataMember=""; bs.DataSource=_dt;   (bs = el BindingSource del Designer)
//   Guardar(): dgv.EndEdit(); bs.EndEdit(); _repo.GuardarX(_dt); recargar.
// .Designer.cs: quitar el DataSet tipado (Ds*) y el *TableAdapter (instanciación, BeginInit/EndInit, bloque de
//   config y campos); CONSERVAR el BindingSource y CONSERVAR las DataGridViewColumn (ver gotcha §11).
```
Pasos: (a) repo ListarX/GuardarX. (b) reescribir form .cs. (c) limpiar .Designer.cs. (d) `<Compile Include>` del repo. (e) build verde. (f) commit del módulo.
Forms NO-mantenimiento (visores/combos/transaccionales): `Consultar<T>`->List<POCO> a grid/combo; `ConsultarTabla`->DataTable para lógica que ya usa DataTable; SPs -> `Ejecutar`/`Consultar` con `commandType: CommandType.StoredProcedure`.

## 8. ⚠️ Gotcha crítico: VarGlobales (estado global)
`ADIGGM\Clases\VarGlobales.cs` tiene instancias ESTÁTICAS GLOBALES de TableAdapters "Consultas" (SPs) usadas en muchos forms (ej. `VarGlobales.consultasOC.OC_AutorizarOrden(...)`):
`consultasTrans`(DsTransporteAdiggm), ~~`consultas`(DsCodeasAdiggm)~~ ✅ **ELIMINADO d80d618** (ya ningún form lo usa), `consultasOC`(DsOC), `consultasOCWeb`(DsOCWeb), `consultasFAC`(DsFAC — ya retirado con DsFAC), `consultasPR`(DsPresupuesto), `consultasCA`(DsCA — ya retirado), `consultasInv`(DsInventarioAdiggm — ya retirado).
Para retirar un DataSet por completo hay que reemplazar TAMBIÉN estas llamadas globales por métodos de repositorio (SP vía Ejecutar/Consultar con `CommandType.StoredProcedure`). Buscar: `grep "VarGlobales.consultas"`.
(VarGlobales también tiene Usuario/IdUsuario/IdPerfil/nombreSistema/rutas — dejar igual.)

## 9. Roadmap DataSets — formularios por DataSet (orden: menos→más; borrar el .xsd cuando TODOS migren)
- **DsInventarioAdiggm**: ✅ ELIMINADO (c2831bf).
- **DsPermisos**: ✅ ELIMINADO (178c74b). frmMenuSistema (4e3c9bb), frmSubMenu (d567100), frmDetSubMenu (81bfa66), frmAsigPermisos + fixes §14.1-4 (d7bf391); fix XML nietos §14.5 (dd8a95b).
- **DsOCWeb**: ✅ **ELIMINADO (9201553)**. Tenía un único consumidor: `VarGlobales.consultasOCWeb.OCWeb_OrdenCompraInsert` en OC\Visores\VisOrdenesTrabajo → migrado a **`RepositorioOC`** (nuevo, Conexion.TRANSPORTE, base del futuro DsOC) método `InsertarOrdenCompraWeb` (SP dbo.OCWeb_OrdenCompraInsert, `Escalar<int>`). `consultasOCWeb` retirado de VarGlobales; .xsd/.Designer.cs/.xsc/.xss + la "Copia en conflicto" de Dropbox borrados y fuera del csproj. QUINTO DataSet retirado por completo (tras DsInventarioAdiggm, DsPermisos, DsCA, DsFAC). VisOrdenesTrabajo sigue usando DsOC/DsTransporteAdiggm (fases siguientes).
- **DsCA**: ✅ ELIMINADO (92c92b3). frmDetProducto, frmDetCredito, frmCarnetImp, frmBuscarAsociados, SAC\frmMenu, frmDevoluciones, frmInformacionAsoc, FrmSolCred (0fc38a3), frmTarjetas y frmObsProducto migrados; consultasCA quitado de VarGlobales. FrmSolCred además dejó de usar DsCodeasAdiggm/consultas (RepositorioCodeas creció: asociados, solicitudes, estado financiero, amortizaciones, fechas de corte, inserts SAC_*).
- **DsFAC**: ✅ **ELIMINADO (914aad8)**. 11 forms migrados a RepositorioFAC+Dapper (FAC_TipoMoneda 9aa7b5f, FAC_CAI e91b4a1, FAC_TipoFacturas 6cf66a7, FAC_Productos 4788aca, FAC_TipoFacUsuarios 68af2ca, FAC_ReporteCierres 8175c5d, SAC\frmClientesRTN f674ef5, FAC_BusquedaViajes 2d50319, FAC_VisorFacturas 7a3b8b9, FAC_ActualizarDatos f8b2698, FAC_Factura dad9c7d) + `consultasFAC` retirado de VarGlobales; .xsd/.xsc/.xss/.Designer.cs borrados y fuera del csproj. Tercer DataSet retirado por completo (tras DsInventarioAdiggm, DsPermisos, DsCA). Patrones nuevos aportados: columna combo en grid, SP→grid+combo, SP con params OUTPUT (DynamicParameters InputOutput), scalar SP (Escalar<T>). `consultasTrans.TR_SelectTarifaRetro` en FAC_Factura se mantiene (es DsTransporteAdiggm). (El XSD tiene otros adapters de FAC_TipoFacturas/FAC_Productos con filtros y fila "Todos" — los usan los forms transaccionales, cubrir al migrarlos.)
- **DsCodeasAdiggm** (15, EN CURSO — conexión TransporteAdiggm, `RepositorioCodeas` ya existe): ✅ Mant\FrmTipoFac (0f27b69, mant. TR_TipoFacturas), ✅ Mant\FrmAsigTpFacTpVeh (b74ef6e, maestro-combo + grid hijo por DataRelation + columna combo TipoVehiculo; patrón frmSubMenu), ✅ Mant\FrmCierreCliente (b8a02a7, combo filtro + grid por SP + 2 cols combo + 3 SPs PR_), ✅ Mant\FrmCierresBuscar (9f93e40, 2 combos selectores + grid de cierres agregados + col combo idCierre→Semana)→RepositorioTransporte, ✅ Mant\FrmSyncTransCod (555dcb5, sync transporte→CODEAS: grid asiento SP + combo tipo asiento + verificación cuenta cross-DB covibase + guardado; PRIMER reemplazo de VarGlobales.consultas)→**RepositorioCodeas** (dominio Codeas/covibase real), ✅ Mant\FrmEstadosDeCuenta (4dae477, COD_* envío de estados de cuenta)→**RepositorioCodeas**: combo Divisiones + grid ListaCorreos por DataRelation (patrón frmSubMenu), ListaCorreos Opción A (recarga tras guardar por identity), RDLC por asociado con los 3 SP ya existentes (CargarASMaestras/EstadoCuenta/EstadoCuentaDet), ✅ SAC\FrmAsocBuscar (b5634bf, diálogo de búsqueda)→**RepositorioCodeas**: BuscarAsociados→SP COD_ASMaestras @Asociado; 34 cols en código (5 visibles), ✅ SAC\frmOrdenes (24f7af6, PARCIAL: solo se quitó DsCodeasAdiggm)→**RepositorioCodeas** CargarASMaestras (nombre del asociado), ✅ IA\frmInformacionAsoc (YA estaba migrado — VERIFICADO 2026-06-23: sin TableAdapter ni DataSet tipado; usa _repoCA/_repoCodeas; los 3 `Ds*` son solo nombres de ReportDataSource del RDLC alimentados por CargarASMaestras/EstadoCuenta/EstadoCuentaDet). **OJO**: FrmTipoFac/FrmAsigTpFacTpVeh/FrmCierreCliente/FrmCierresBuscar usan `TR_*` de TransporteAdiggm → **RepositorioTransporte** (regla §4). ✅ Visores\FrmVisorPrestamos (09f803d)→**RepositorioCodeas**: BuscarSolicitudesVisor (SP SAC_SolicitudesVisor), ReporteSolicitudes (SP PR_R_Solicitudes, Excel), AprobarSolicitud/AnularSolicitud (reemplazan VarGlobales.consultas); reportes reusan CargarAsociadoPorId/CargarSolicitud/CargarEstadoFinanciero/CargarAmortizaciones; grid 29 cols en código. ✅ SAC\Visores\frmVisorOrdenesSAC (d80d618)→RepositorioCodeas.BuscarOrdenesSac (SP SAC_BuscarAsoc; grid 10 cols en código). ✅ **`VarGlobales.consultas` ELIMINADO (d80d618)** — los 2 últimos `PR_VerificarCtaCont` (INV\frmVisorInventarioSync, OC\VisOCCodeas) usan ahora `_repoCodeas.VerificarCuentaContable` (SELECT idéntico al query Text del adapter). **Cierre de DsCodeasAdiggm (estado 2026-06-23): ya NINGÚN form lo usa salvo (1) Reportes\RptMaestro → dominio DsTransporteAdiggm, se cierra en la fase Transporte; (2) PRESUPUESTO\frmPresupuestoSem → DIFERIDO.** El `.xsd` NO se puede borrar hasta cerrar esos dos. Achicar DsCodeasAdiggm = HECHO salvo lo Transporte/diferido. **frmOrdenes sigue en el roadmap de DsOC** (OC_Proveedores + consultasOC.SAC_OrdenesInsert).
- **DsPresupuesto** (25, ⚠️ DIFERIDO POR EL USUARIO 2026-06-15): NO migrar aún — el usuario rehará el módulo de Presupuesto (no se usa en producción todavía) y lo mejorará luego. Saltarlo en el orden. Cuando se retome: todo PRESUPUESTO\* (frmAños/frmMeses/frmSemanas/frmCargos/frm_) + VarGlobales.consultasPR. (Muchos mantenimientos de catálogo simples → buen lote tras INV.)
- **DsOC** (29, EN CURSO — conexión TransporteAdiggm; **`RepositorioOC`** ya creado, Conexion.TRANSPORTE; tablas con prefijo `CP_`/`OC_`): ✅ OC\Mantenimiento\ManTipoDocumento (fffc9be, Opción A, catálogo CP_TipoDocumentos → ListarTiposDocumento/GuardarTiposDocumento). Pendientes: OC\Man*(ManProductos/ManProveedores/ManResponsables/ManCatProductos/ManDepartamentos/ManTipoOC/ManParametrizacion/ManAsigCuentas — los `Man*` son catálogos Opción A simples, buen lote; ManProveedores/ManAsigCuentas usan consultasOC), OC\Tran*(TranAbonar/TranConfirmarOrden/TranOrdenCompra), OC\Vis*(VisAbonos/VisCxP/VisCambioAceite/VisOCCodeas/VisOCCodeasDet/VisOCConfirmadas/VisOrdenesTrabajo/VisProveedores), OC\Reportes\*, Herramientas\frmDigitarEstadoCuentaBCO, INV\frmInventario, INV\frmVisorExistencias (ya migrados de DsInventario; revisar si tienen DsOC), SAC\frmOrdenes (parte OC pendiente) + VarGlobales.consultasOC. **OJO**: muchos adapters CP_* tienen FillByActivos/FillByTodos (fila "TODOS") para combos de los transaccionales — cubrir al migrar esos.
- **DsTransporteAdiggm** (35, núcleo de transporte; dejar al final): casi todo Mantenimiento\* (FrmMotoristas/FrmVehiculos/FrmRutas/FrmTarifaRutas/FrmAsigTarifas/FrmClientes/FrmContratistas/FrmFincas/FrmZonas/FrmBloques/FrmLagunas/FrmCierres...), Transaccionales\FrmViajes(+Retro), Visores\FrmVisorViajes, Seguridad\FrmMantUsuarios/FrmCrearUsuarios/FrmEditUsers + VarGlobales.consultasTrans.

## 10. Pendiente (otras fases)
- **SEGURIDAD**:
  - ✅ **Credenciales externalizadas (7e147f2, 2026-06-15)**: TODAS las credenciales hardcodeadas movidas a `appSettings` y leídas por `ADIGGM\CapaDatos\AppConfig.cs`. El `App.config` versionado tiene las claves VACÍAS + `<appSettings file="secrets.config">`; los valores REALES viven solo en `ADIGGM\secrets.config` (**gitignored**, se copia a la salida por el csproj con `Condition=Exists`). Plantilla: `secrets.config.example`. Claves: `ReportServerUsuario/Clave/Dominio` (SSRS, en FAC_ReporteCierres/FAC_VerReporte/ReporteMaestro/VisualizarReporte/TrazabilidadVehiculo), `SmtpUsuario/Clave` (serviciosadiggm@, en MdiPrincipal/VisOrdenesTrabajo/TranOrdenCompra), `SmtpEdoCtaUsuario/Clave` (jlanza@, en FrmEstadosDeCuenta), `TwilioAccountSid/AuthToken`. `CustomReportCredentials.cs` ya recibía por parámetro y no se instancia (código muerto).
  - ⚠️ **PENDIENTE — ROTAR (usuario)**: los valores reales SIGUEN en el historial git (SSRS `Administrator/camaron+2016`, SMTP `serviciosadi@2020` y `JuniorADI@19`). Externalizar NO los borra del pasado: hay que CAMBIARLOS en el dominio/SSRS/Office365. Además el token de Twilio. Al desplegar (ClickOnce): incluir `secrets.config` junto al `.exe`.
  - (c) Cifrar `<connectionStrings>` (aspnet_regiis -pe) y/o dejar de usar el login `sa` (siguen en App.config; sería el siguiente paso de esta fase).
- **LIMPIEZA**: borrar ~20 archivos "Copia en conflicto de ..." de Dropbox (no están en el csproj); quitar código muerto (línea comentada `MdiPrincipal.cs` ~341); consolidar 4 libs de Excel (ClosedXML/EPPlus/ExcelDataReader/Interop.Excel); migrar `System.Data.SqlClient` → `Microsoft.Data.SqlClient` (ya referenciado, sin usar; OJO: 4+ pone Encrypt=true por defecto).

## 11. Gotchas / lecciones
- ⚠️ **GRIDS Y EL DISEÑADOR DE VS** (causa raíz hallada 2026-06-10): si el `.Designer.cs` deja `dgv.DataSource = <BindingSource>` y el BindingSource ya NO tiene DataSet de diseño (migrado), al abrir el form en el diseñador VS no puede resolver el esquema y ELIMINA las `DataGridViewColumn` EN DISCO (deja `AutoGenerateColumns=false` + 0 columnas → grid VACÍO en runtime, pero COMPILA; también toca el .resx). RECURRENTE hasta quitar el binding de diseño. **Patrón obligatorio al migrar forms con grid: QUITAR `dgv.DataSource = bs` del Designer y asignarlo en código en CargarX() después de `bs.DataSource = _dt`** (hecho en frmTipoOp/frmBodegas, commit 3774396).
  - ⚠️ **AÚN ASÍ el diseñador puede borrar columnas** (confirmado 2026-06-16 en FrmCierresBuscar: al abrir el form en el diseñador, VS borró TODAS las columnas del grid incl. la combo; en runtime `idCierre.DataSource=...` tronó con NullReferenceException). Quitar el `dgv.DataSource` de diseño NO basta. **SOLUCIÓN DEFINITIVA (2026-06-16): definir las columnas del grid EN CÓDIGO, no en el Designer.** Se quitan las columnas (creación/config/AddRange/campos) del `.Designer.cs` y se construyen en un método `ConfigurarColumnas()` llamado desde el constructor tras `InitializeComponent()`. Como el diseñador NO tiene columnas en InitializeComponent, no puede borrarlas → **el form queda inmune; ya NO hay que restaurar de git**. Helper `Clases/GridColumnas.cs` (`Texto/Check/Combo`, ReadOnly por defecto; grids editables de mantenimiento usan el default y la edición entra por cascada de `dgv.ReadOnly=false`). Las columnas combo reciben su `DataSource` en el Load tras poblar su BindingSource. **Aplicado en TODO el lote Codeas/Transporte**: FrmCierresBuscar, FrmCierreCliente, FrmAsigTpFacTpVeh, FrmTipoFac, FrmSyncTransCod (commits 5c1a727/8b14cad/cd7aca5). PENDIENTE migrar al mismo patrón los grids FAC ya hechos (FAC_Productos, FAC_Factura, FAC_VisorFacturas, FAC_TipoMoneda, FAC_CAI, FAC_TipoFacturas) y los de fases anteriores — hacerlo cuando se toquen o si el usuario los abre en el diseñador. **De aquí en adelante: todo grid migrado define columnas en código.** Tras tocar un `.Designer.cs` de grid, verifica `dgv.Columns.AddRange(...)`, bloques `// <col>` con `DataPropertyName` y campos de columna; si se perdieron, restaura de git (`git show HEAD:<ruta>`). Las columnas usan `DataPropertyName` = nombre de columna del DataTable; el grid se habilita con `dgv.ReadOnly=false` (cascada a columnas).
- ⚠️ **Bindings de diseño huérfanos** (regresión hallada 2026-06-12 en FrmSolCred, fix 2304c3a): al quitar el DataSet de diseño de un BindingSource, los `control.DataBindings.Add(..., bindingSource, "columna", ...)` que el Designer tenga contra ese BindingSource quedan sin esquema y lanzan ArgumentException ("No se puede enlazar la propiedad o la columna X... dataMember") al ABRIR el form. Al migrar: `grep "DataBindings.Add" <Form>.Designer.cs`; los huérfanos se quitan del Designer y se recrean en runtime DESPUÉS de asignar el DataTable al BindingSource (con `DataBindings.Clear()` antes para no duplicar en recargas).
- Antes de crear un POCO: `grep "class <Nombre>"`. Ya existen `PoliticaPago` y `RangoHora` en `ADIGGM\Clases\CalculadoraHoras.cs` (REUTILIZAR; PoliticaPago se extendió con PoliticaID/NombrePolitica). Para guardar horas se usa tupla `(TimeSpan,TimeSpan)`.
- NO usar regex greedy sin anclar en archivos con patrones repetidos (corrompió frmAsistencias; se restauró con git y se reescribió completo).
- `DataTable.Load` infiere columnas no-nulas; el PK identity se ignora en el INSERT y se obtiene al recargar.
- ⚠️ `DataTable.Load` también marca `DataColumn.ReadOnly=true` en columnas CALCULADAS (expresiones/CASE de SPs o SELECTs). Si esa columna se edita en un grid (ej. flag Habilitado de usp_CargarPermisos), el binding lanza "columna enlazada a un campo de solo lectura..." y los `Cells[x].Value=` fallan. Fix: en el repositorio, tras ConsultarTabla, `tabla.Columns["X"].ReadOnly = false` (hecho en CargarPermisosUsuario, commit a7f46ba). Revisar esto en CUALQUIER grid editable que se cargue vía Dapper.
- Reescribir historia git: `git filter-repo` NO está en PATH → `python <site-packages>\git_filter_repo.py` en un CLON temporal FUERA de Dropbox; force-push; luego en el repo Dropbox: `git -c transfer.unpackLimit=999999 fetch` + `git reset --hard origin/master`.
- Commit POR MÓDULO; terminar mensajes con `Co-Authored-By: Claude <noreply@anthropic.com>`. No usar `--no-verify`.
- Editar archivos con PowerShell: usar lectura/escritura UTF-8 explícita (no corromper ñ/á).

## 12. Validación pendiente del usuario
`frmTipoOp` (INV→"Tipo Operación") y `frmBodegas` (INV→"Bodegas"): que el listado muestre COLUMNAS; Nuevo+Guardar (INSERT); Editar+Guardar (UPDATE); Cancelar.
`frmTipoOp`/`frmBodegas`/`frmVisorExistencias`/`frmInventario`: VALIDADOS OK 2026-06-10.
`OC→TranConfirmarOrden`: que el combo Bodega cargue (ahora vía repo); el resto del form no cambió.
`Seguridad→Menú Sistema` (frmMenuSistema): listado con columnas; Nuevo+Guardar; Editar+Guardar; Cancelar.
`Seguridad→SubMenú` (frmSubMenu): combo Menú Padre filtra el grid; Nuevo hereda el IdMenu del padre seleccionado; Guardar/Cancelar.
`Seguridad→DetSubMenú` (frmDetSubMenu): cadena Menú→SubMenú→grid; Nuevo hereda el IdSubMenu; Guardar/Cancelar.
`Seguridad` (los 4 forms) + menú del MDI: VALIDADOS OK 2026-06-10 (incl. fix Habilitado a7f46ba).
`IA→frmDetProducto`: VALIDADO OK 2026-06-11.
`IA→frmDetCredito` (se abre desde el detalle del asociado): encabezado del crédito completo (fechas/montos), los 4 tabs cargan (Mov. Aplicados / Tránsito / Pendientes / Aplicados), y % saldo / % plazo / pagos restantes correctos.
**Respaldo por IP (VPN)**: VALIDADO OK 2026-06-11 (probado por el usuario con VPN).
`IA→Carnets` (frmCarnetImp): listado con fotos carga; marcar/desmarcar; Exportar a Excel (probar CANCELAR el diálogo: ya no debe quedar Excel.exe en el Administrador de tareas); "Imprimir carnets" marca los seleccionados y recarga.
`IA→Buscar Asociados` (frmBuscarAsociados): búsqueda con filtros/orden funciona; Enter en el grid o Aceptar abre la información del asociado; Aceptar sin resultados ya no truena.
`SAC→Menú` (frmMenu): los 3 tabs cargan grid+imagen+reporte; Editar→cambiar platos/fecha/activo→Guardar persiste (incl. el último cambio sin salir de la celda); cambiar foto y Guardar; Exportar reporte: la opción PNG genera PNG y BMP genera BMP.
`Herramientas→Devoluciones` (frmDevoluciones): combo Caja carga; Verificar muestra originales+corregidos con colores; Corregir con cuentas inválidas AVISA (antes no hacía nada); Corregir válido actualiza (todo-o-nada).
`IA→Información del Asociado` (frmInformacionAsoc, CENTRAL — probar a fondo): encabezado+foto+tiempo afiliado; grids Productos/Créditos con Tránsito/Pendientes y totales; filtros con saldo/todos; detalle de producto y de crédito; panel PIN (validar/crear/renovar/reportar/activar/desactivar — frmTarjetas guarda vía repo ahora); personas autorizadas (Nuevo/Editar/Guardar/Cancelar/bloquear); Estado de Cuenta PDF; Obs de producto (frmObsProducto).
`FAC→Tipo Moneda` (FAC_TipoMoneda): listado con columnas (Id oculto, TipoMoneda, Simbolo, ValorLempiras N2); Nuevo+Guardar (INSERT); Editar+Guardar (UPDATE); Cancelar recarga.
`FAC→CAI` (FAC_CAI): listado con columnas (Cai, FragmentoSAR, fechas, números, check Activo); Nuevo+Guardar; Editar+Guardar; Cancelar; validación de celdas vacías sigue avisando.
`FAC→Tipo Facturas` (FAC_TipoFacturas): listado con columnas (Cod, TipoFactura, checks Activo/EsTransporte); Nuevo+Guardar; Editar+Guardar; Cancelar.
`FAC→Productos` (FAC_Productos): listado con columnas; las DOS columnas combo (Tipo=FAC_TipoEx, Tipo Factura=TR_TipoFacturas) muestran el texto correcto y se pueden elegir al editar; Nuevo+Guardar; Editar+Guardar (verificar que el combo persiste el Id correcto); Cancelar.
`FAC→Tipo Fac Usuarios` (FAC_TipoFacUsuarios): el combo Tipo Factura carga; al elegir uno se llenan los dos grids (no-asignados / asignados); búsqueda con Enter en cada caja filtra su grid; marcar usuarios en "no asignados" + Agregar los mueve a "asignados"; marcar en "asignados" + Eliminar los devuelve; ambos grids se refrescan.
`FAC→Reporte de Cierres` (FAC_ReporteCierres): el combo Cliente/Finca carga; elegir cliente + fechas Desde/Hasta + Visualizar genera el reporte de SSRS (ServerReport) correctamente.
`SAC→Clientes RTN` (frmClientesRTN): pestaña "Ingresar RTN" lista los clientes (RTN/Empresa/Dirección/Contacto/Teléfono); Nuevo+Guardar (INSERT con RTN como PK); Editar+Guardar; Cancelar; pestaña de búsqueda filtra por RTN o Empresa (Enter o botón Buscar). Probar que NO se rompe al renombrar un RTN existente (no persiste, es esperado).
`FAC→Búsqueda de Viajes` (FAC_BusquedaViajes, se abre desde FAC_Factura con un IdCliente): cargan los 5 combos (Tipo Factura, Producto—depende del tipo, Calendarización/Cierre, Cliente—preseleccionado y deshabilitado, Proforma—depende del cierre); Visualizar llena el grid de boletas y calcula Cantidad/ISV/Sub-Total/Total; Aceptar valida selección y pasa los datos a FAC_Factura.
`Mant→Tipo Factura` (FrmTipoFac, tipos de factura de transporte): listado (TipoFactura, check Activo); Nuevo+Guardar (INSERT); Editar+Guardar (UPDATE); Cancelar recarga.
`Mant→Asignar Tipo Fac/Tipo Veh` (FrmAsigTpFacTpVeh): el combo "Filtrar Por: Tipo de Factura" cambia el grid (relación FK); el grid muestra los tipos de vehículo asignados a esa factura (columna combo); Nuevo agrega una fila que **hereda el IdTipoFactura del combo** y deja elegir el tipo de vehículo; Guardar persiste (INSERT/UPDATE/DELETE); Editar/Cancelar OK. Probar cambiar de tipo de factura en el combo y que el grid se filtre.
`Mant→Buscar Cierres` (FrmCierresBuscar, diálogo de selección): los combos Cliente (activos) y Tipo de Factura llenan el grid (cierres CERRADOS, no anulados/sincronizados, agregados por cierre; columna Cierre=Semana, fechas, montos); con resultados se habilita Seleccionar, que devuelve el cierre elegido al llamador (contrato.Ejecutar) y cierra con DialogResult.OK.
`Mant→Sincronizar Trans→CODEAS` (FrmSyncTransCod): Buscar Cierre abre FrmCierresBuscar y al elegir uno llena el grid del asiento contable (cuenta/descr/debe/haber); Debe/Haber/Diferencia se calculan (Dif≠0 en rojo); el combo Tipo Asiento carga; Verificar Cuentas valida cada cuenta contra covibase (avisa si no existe y deshabilita Sync); con todo OK, Sincronizar guarda el asiento en CODEAS y limpia.
`Mant→Cierre de Cliente` (FrmCierreCliente, se abre con un IdCierre): el combo "Seleccione" (Tipo de Factura) filtra el grid; el grid lista los cierres por cliente (columnas combo Cliente y Tipo Vehículo, montos, checks Cerrado/SynCodeas/Anulado); seleccionar una fila recalcula los totales (Subtotal/ISV/Total) del cliente; clic derecho según estado: NO cerrado→Cerrar; cerrado→Reversar Cerrado + (Aplicar ISV si ISV=0 / Borrar ISV si ISV>0); las 3 acciones piden confirmación, ejecutan el SP y recargan.
`FAC→Nueva Factura` (FAC_Factura, alta — probar a fondo): el combo Tipo Factura carga (solo los del usuario); al elegir uno **de transporte** aparece "Buscar Boletas", se deshabilita el detalle manual y el combo Cliente filtra a clientes de transporte; al elegir uno **no-transporte** se habilita el detalle manual (combo Servicio/Cantidad/Precio + Agregar). CAI/correlativo se autollenan; ISV carga; el check "Aplica ISV" refleja si el cliente paga ISV. Agregar valida duplicados/cantidad/precio y suma en el grid; Quitar elimina línea. Guardar valida (tipo/cliente/moneda/≥1 detalle), pide confirmación, inserta y (si CAI disponible) ofrece ver la factura; si no hay números de CAI avisa. Retroexcavadoras: el precio sale de TR_SelectTarifaRetro.
`FAC→Actualizar Datos` (FAC_ActualizarDatos, se abre desde el menú contextual del visor): al abrir, los 3 campos (N° Orden, N° SAG, N° Proforma) cargan con los valores actuales de la factura (Opcion=2/lectura por OUTPUT); editar + Guardar persiste (Opcion=1) y muestra "Datos actualizados!".
`FAC→Visor de Facturas` (FAC_VisorFacturas): combos Tipo Factura y Cliente con "Todos"; Visualizar/Enter/fechas filtran el grid maestro; al seleccionar una factura carga su detalle (servicios) y muestra las Observaciones en el textbox; clic derecho sobre una factura NO anulada muestra el menú (Anular/Ver/Actualizar; "Ver con descripción" solo si es Factura Transporte); Anular pide confirmación y refresca; Nuevo abre FAC_Factura y al cerrar refresca.
`SAC→Solicitud de Crédito` (FrmSolCred, CENTRAL — probar a fondo): **el form ya ABRE (fix 2304c3a: bindings de diseño huérfanos); verificar que años/meses/días se llenan al buscar asociado**; buscar asociado por identidad (grids Aportes/Créditos con Tránsito/Pendientes — **en modo EDITAR los montos T/P ya no se inflan acumulándose entre filas**); totales y posición financiera; tipos de solicitud (préstamo/refinanciamiento/retiro/neteo); fechas de corte y formalización; Generar (guarda asociado+solicitud+amortización+estado financiero y abre PDF); Imprimir (juego completo de reportes a impresora); Estado de cuenta. **§13.b (columnas en código, b7cc00e): VALIDADO OK 2026-06-22 por el usuario** (los 4 grids muestran columnas, Marcar/seleccionado funcionan).
`HE→Editar Asistencia` (frmEditarAsistencia, §13.b 9c511d8 — PENDIENTE VALIDAR): que el grid de tiempos (Hora de Inicio / Hora Final) muestre las 2 columnas; agregar/editar horas (formato hora) y Guardar persiste; con período CERRADO el grid sale bloqueado (no editable) y Guardar deshabilitado.
`Mant→Estados de Cuenta` (FrmEstadosDeCuenta, §13.a 4dae477): **VALIDADO OK 2026-06-23** (el "no enviaba" era que el asociado no tenía marcada la casilla Activo → el loop solo envía marcados Y activos; ahora un resumen final lo avisa, ae5d7e7). Combo Divisiones FILTRA el grid (relación IdDivision); Nuevo/Editar/Guardar/Cancelar; marcar + Enviar/Imprimir genera el RDLC por asociado marcado y activo. SMTP depende de secrets.config (jlanza@).
`SAC→Buscar Asociado` (FrmAsocBuscar, §13.a b5634bf — PENDIENTE VALIDAR): al abrir lista todos los asociados; escribir en la caja filtra (SP COD_ASMaestras); Seleccionar devuelve el asociado elegido al llamador (contrato.Ejecutar) y cierra; Seleccionar sin resultados/selección ya no truena.
`SAC→Órdenes` (frmOrdenes, §13.a 24f7af6 — PENDIENTE VALIDAR): al escribir la identidad y salir del campo, txtNombreCompleto muestra el nombre del asociado (vía repo; vacío si no existe → "El asociado no existe" al Agregar). El resto del flujo (combo Proveedores, Agregar a la grilla, Guardar) NO cambió (sigue en DsOC).
`Visores→Visor de Préstamos` (FrmVisorPrestamos, §13.a 09f803d — PENDIENTE VALIDAR): al abrir, el grid lista las solicitudes (filtros por Código / rango de fechas / rango de N° solicitud + Dependencia → Visualizar); seleccionar una fila llena los detalles; clic derecho según estado → Aprobar/Reversar aprobación/Anular/Reversar anulación (refresca el grid); Editar abre FrmSolCred; Imprimir/Generar produce los reportes RDLC (préstamo/refinanciamiento/retiro parcial/neteo/readecuación) según TipoSolicitud; Exportar a Excel genera el listado. (Mismos SP/credenciales; solo cambió la capa de datos.)
`SAC→Visor de Órdenes SAC` (frmVisorOrdenesSAC, §13.a d80d618 — PENDIENTE VALIDAR): al abrir y al Buscar (o Enter en la caja), el grid lista las órdenes SAC filtradas por el texto + combos Orden/Operador; columnas correctas (# Orden, Fecha, Proveedor, Identidad, Nombre, Tipo de Orden, Valor, Estado).
`Herramientas/INV→Sync Inventario` (frmVisorInventarioSync) y `OC→Visor OC Codeas` (VisOCCodeas) (§8 d80d618 — PENDIENTE VALIDAR): la verificación de cuentas contables (antes VarGlobales.consultas.PR_VerificarCtaCont, ahora _repoCodeas.VerificarCuentaContable) sigue avisando cuando una cuenta NO existe en covibase (==0) igual que antes.
`OC→Visor Órdenes de Trabajo` (VisOrdenesTrabajo, DsOCWeb 9201553 — PENDIENTE VALIDAR): la opción de solicitar (rama que NO usa la URL web, sino el SP local OCWeb_OrdenCompraInsert) muestra "solicitud enviada satisfactoriamente" cuando el SP devuelve 1, y el aviso de "ya existe una solicitud en proceso" en caso contrario. El resto del form (DsOC/DsTransporte) no cambió.
`OC→Tipo de Documento` (ManTipoDocumento, DsOC fffc9be — PENDIENTE VALIDAR): el listado muestra columnas (Código, TipoDocumento, check Activo); Nuevo+Guardar (INSERT); Editar+Guardar (UPDATE); Cancelar recarga. (Primer form de DsOC; guardado ahora transaccional.)

## 13. TAREA INMEDIATA
> **HANDOFF (último commit de código `fffc9be` = OC\ManTipoDocumento, primer form de DsOC. Working tree limpio salvo posible `HE/frmFeriados.resx` = VS recodificando base64, NO commitear. `git add` SELECTIVO siempre, NUNCA `git add -A`).** **§13.b ✅ COMPLETO AL 100%.** **DataSets ELIMINADOS por completo (5): DsInventarioAdiggm, DsPermisos, DsCA, DsFAC, DsOCWeb.** DsCodeasAdiggm achicado (solo RptMaestro/Transporte y frmPresupuestoSem/diferido lo usan). **EN CURSO: DsOC** (29 forms, conexión TransporteAdiggm, `RepositorioOC` ya existe; tablas `CP_`/`OC_`). ✅ ManTipoDocumento (fffc9be, Opción A). **SIGUIENTE: seguir con los catálogos `Man*` de OC** (ManTipoOC, ManDepartamentos, ManCatProductos, ManParametrizacion, ManResponsables, ManProductos — Opción A simples; ManProveedores/ManAsigCuentas usan consultasOC; ManProductos/ManProveedores tienen combos), luego Tran*/Vis*/Reportes y `VarGlobales.consultasOC`. PRESUPUESTO DIFERIDO. **Recordar para §13.a**: un form por turno, análisis de errores de lógica (§0; graves se reparan, latentes se documentan estilo §14), repo coherente con la BD del dato (§4/§9 regla), gotchas §8 (VarGlobales.consultas) / §11 (grid sin DataSource de diseño + bindings huérfanos), y como §13.b ya cerró, TODO grid migrado define columnas en código (helper GridColumnas) + toggle con GridColumnas.Edicion (§14.10) DESDE EL INICIO. **OTRAS TAREAS RECIENTES (paralelas, ya entregadas)**: (1) **§13.c feature `FrmActualizarTarifas` COMPLETA** (form + repo + auditoría genérica `ADI_Auditoria`; pendiente del usuario: crear la tabla con el script SQL, agregar ítem de menú al MDI, validar; incluye modo % Reversible/Directo, ruta=Origen+' - '+Destino). (2) **Fix despliegue (c0b91dd)**: credenciales SSRS movidas INLINE a App.config (reportes ya no dependen de secrets.config; usuario autorizó exponerlas — SIGUE PENDIENTE ROTARLAS, ahora más expuestas) y `<UpdateUrl>` ClickOnce cambiado de `\\ADIGGM` a `\\192.168.2.77` (instalación/upgrade por VPN); requiere RE-PUBLICAR. (3) **Blindaje reporte (b4c5f26)**: `VisualizarReporte_Load` en try/catch (un fallo SSRS ya no tira "excepción no controlada"; avisa si faltan credenciales). El SMTP sigue dependiendo de secrets.config (si fallan correos en clientes, aplicar el mismo inline). (4) **SmartScreen "aplicación desconocida" (pendiente, requiere $$)**: el ClickOnce se firma con `ADIGGM_TemporaryKey.pfx` (cert TEMPORAL autogenerado por VS, no confiable) y el .exe no tiene Authenticode → Windows lo marca. NO se arregla con código: requiere un **certificado de firma de código** de CA reconocida (EV = quita el aviso ya; OV = gana reputación con el tiempo) y firmar .exe + manifiestos. Cuando el usuario lo consiga, configurar el csproj (`ManifestKeyFile`/`ManifestCertificateThumbprint` + signtool del .exe). Interino sin costo: "Más información → Ejecutar de todas formas", o que IT lo permita por GPO de Defender/SmartScreen. El usuario ya validó FrmActualizarTarifas OK, creó ADI_Auditoria (con datos) y agregó el ítem de menú; re-publicó los cambios de despliegue. **Recordar**: toggle de edición SIEMPRE con `GridColumnas.Edicion(dgv,bool)` (§14.10); preservar Names/ORDEN si el .cs referencia celdas; verificar formatos vía `cellStyle` y NO borrar los cellStyle del GRID; ⚠️ columnas genéricas `dataGridViewTextBoxColumnN` van con **d minúscula** (case-sensitive en el borrado) — verificar balance de llaves tras limpiar (§0.f). **BARRIDO EXHAUSTIVO confirmado (2026-06-17)**: el universo de forms migrados con grid vulnerable es cerrado y está completo en este checklist; lo único pendiente es el lote **IA/CA** (frmDetProducto, frmDetCredito, frmBuscarAsociados, frmCarnetImp, frmInformacionAsoc, SAC\frmMenu, SAC\frmClientesRTN, SAC\FrmSolCred) + **HE\frmEditarAsistencia**. **Siguiente: lote IA/CA** con el mismo método de §13.b. La migración de DataSets (§13.a) está en pausa hasta terminar §13.b. Arranque sugerido para la nueva sesión: leer §0 (eficiencia), §11 (gotcha grids + helper GridColumnas) y este §13.

### 13.b BARRIDO §11 — columnas de grid EN CÓDIGO en TODO form ya migrado (EN CURSO, prioridad del usuario "para que no se nos pasen por alto")
Mover las columnas del `.Designer.cs` a `ConfigurarColumnas()` (helper `Clases/GridColumnas.cs`) en cada form MIGRADO con grid de columnas manuales, para inmunizarlos al borrado del diseñador de VS. Checklist (✅ hecho / ⬜ pendiente):
- ✅ FrmCierresBuscar, FrmCierreCliente, FrmAsigTpFacTpVeh, FrmTipoFac, FrmSyncTransCod (5c1a727/8b14cad/cd7aca5)
- ✅ **MÓDULO FAC COMPLETO**: FAC_TipoMoneda, FAC_TipoFacturas, FAC_CAI (a73f610), FAC_Productos, FAC_TipoFacUsuarios (8672256), FAC_VisorFacturas (4d4ea58), FAC_BusquedaViajes, FAC_Factura (02b2739)
- ✅ **INV**: frmTipoOp, frmBodegas (9bb1ac3). frmVisorExistencias = sin grid (usa RDLC) → N/A; frmInventario = grid NO enlazado (Rows.Add posicional, sin DataSource/DataPropertyName, celdas por Name + LinkColumn `quitar`) → no vulnerable al gotcha §11 (el diseñador solo borra columnas de grids con DataSource de diseño sin esquema), NO se toca.
- ✅ **Seguridad**: frmMenuSistema, frmSubMenu, frmDetSubMenu, frmAsigPermisos (2728ffc). En frmAsigPermisos solo `habilitado` es editable (`Columns["habilitado"].ReadOnly=false` en cargarDgv). `GridColumnas.Check` extendido con width/autoSize.
- ✅ **IA/CA COMPLETO (8/8)**: ✅ SAC\frmClientesRTN (7a92bf9), ✅ frmCarnetImp (f6662df), ✅ frmDetProducto (82a6b8f), ✅ SAC\frmMenu (cb3e9da), ✅ frmBuscarAsociados (5ba07a7), ✅ frmDetCredito (8a03ca8, 4 grids 55 cols), ✅ frmInformacionAsoc (d8e4e1e, 3 grids 81 cols; el .cs referencia muchas cols por Name → Names exactos; Transito/Pendientes no enlazadas; conservado cellStyle22), ✅ **SAC\FrmSolCred (b7cc00e, 4 grids 110 cols)**. FrmSolCred: grids = `dgvAportes`(27, grid ReadOnly) + `dgvCreditos`(47, grid editable) + `dgvAportesEdit`(15, grid ReadOnly) + `dgvCreditosEdit`(21, grid editable); `ConfigurarColumnas()` con 4 submétodos (tras InitializeComponent). El `.cs` referencia 46 cols por `Cells["..."]` → Names EXACTOS preservados (incl. sufijos `...DataGridViewTextBoxColumn` y casing como `cCODIGOGES` vs `ccodigogest` en grids distintos; verificado `.Name`==nombre de campo en TODAS). Solo los checkbox de selección quedan editables (readOnly:false): `Marcar` (dgvCreditos, NO enlazado) y `seleccionado` (dgvCreditosEdit); `Transito`/`Pendientes`/`Transito1`/`Pendientes1` = TextBox computadas sin DataPropertyName. NO hay toggle `dgv.ReadOnly` ni `GridColumnas.Edicion` (son grids visor/selección, no mantenimiento). Conservados cellStyle 1/5/6/12 (ColumnHeaders/Default de los 2 grids visibles); eliminados los de columna (2,3,4,7-11,13-19, solo N2). Borradas además 2 declaraciones de columna HUÉRFANAS (descripciDataGridViewTextBoxColumn, cuotaDataGridViewTextBoxColumn1 — nunca creadas/usadas). NO se tocaron los `DataBindings` (fix bindings huérfanos 2304c3a, §11). Build verde.
- ✅ **HE: frmEditarAsistencia (9c511d8)** — dgvTiempos 2 cols (colInicio/colFin), TextBox NO enlazadas (Rows.Add posicional, celdas por Name), Format hora "t", **editables** (readOnly:false; el usuario digita horas); el grid se bloquea entero con `dgvTiempos.ReadOnly=true` si el período está cerrado (one-way, sin re-enable → no aplica §14.10); `AutoSizeColumnsMode=Fill` queda en el grid. Los demás HE usan AutoGenerateColumns → ya inmunes.
- ✅✅ **§13.b COMPLETO AL 100%** (2026-06-22): Seguridad 4 + IA/CA 8 + INV 2 (+ frmInventario/frmVisorExistencias N/A) + frmEditarAsistencia. Todo grid migrado define ya sus columnas en código. **Se reanuda §13.a (migración de DataSets, DsCodeasAdiggm).**
- **BARRIDO EXHAUSTIVO HECHO (2026-06-17)**: crucé `Columns.AddRange` (82 forms con columnas en diseño) contra `TableAdapter`/DataSet tipado en el Designer. Los ÚNICOS forms MIGRADOS con grid de columnas manuales en diseño son los de este checklist (Seguridad 4 + IA/CA 8 + frmEditarAsistencia). N/A: frmInventario (grid NO enlazado), frmVisorExistencias (sin grid/RDLC). Ningún form migrado quedó con grid vaciado por el diseñador (verifiqué `AutoGenerateColumns=false` sin columnas). El resto (OC, PRESUPUESTO, Transporte, Herramientas, FrmAsocBuscar/frmOrdenes/frmVisorOrdenesSAC, FrmEstadosDeCuenta…) sigue con DataSet tipado → fuera de §13.b, se trata al MIGRARLO (§13.a). `SAC\FrmAsocBuscar` se quitó de aquí: NO está migrado (usa dsCodeasAdiggm+TableAdapter). `TranConfirmarOrden`: grid aún en DataSet tipado → N/A.
Método por form: (a) `ConfigurarColumnas()` en el .cs (constructor, tras InitializeComponent) con `GridColumnas.Texto/Check/Combo` replicando Name/DataPropertyName/HeaderText/Visible/Format/Width/AutoSizeMode; (b) en el Designer quitar creación de columnas, `AutoGenerateColumns=false`, `Columns.AddRange`, bloques `// <col>`, cellStyles locales y los campos `private ... <col>;`; (c) columnas combo: su `DataSource` se asigna en el Load (ya existe). Mantenimientos editables: ⚠️ **NO usar `dgv.ReadOnly = false` directo en btnNuevo/btnEditar** — con columnas en código el cascade falla al 1er clic (ver §14.10); usar **`Clases.GridColumnas.Edicion(dgv, true/false)`**. Build verde + commit por lote.

### 13.a Migración de DataSets (REANUDADA — §13.b ya cerrado)
**DsFAC ✅ COMPLETO. DsPresupuesto DIFERIDO por el usuario (lo rehará).** En curso: achicar **DsCodeasAdiggm** (`RepositorioCodeas : RepositorioBase(Conexion.TRANSPORTE)` ya existe). ✅ Mant\FrmTipoFac (0f27b69)→RepositorioTransporte, ✅ Mant\FrmAsigTpFacTpVeh (b74ef6e)→RepositorioTransporte, ✅ Mant\FrmCierreCliente (b8a02a7)→RepositorioTransporte, ✅ Mant\FrmCierresBuscar (9f93e40)→RepositorioTransporte, ✅ Mant\FrmSyncTransCod (555dcb5)→RepositorioCodeas, ✅ Mant\FrmEstadosDeCuenta (4dae477; fixes 42b3097 relación en código + ae5d7e7 errores de envío visibles)→RepositorioCodeas (combo Divisiones + grid ListaCorreos por DataRelation; reusa los 3 SP de estado de cuenta), ✅ SAC\FrmAsocBuscar (b5634bf)→RepositorioCodeas (BuscarAsociados→SP COD_ASMaestras; 34 cols en código; guarda btnSeleccionar), ✅ SAC\frmOrdenes (24f7af6, PARCIAL — solo se quitó DsCodeasAdiggm; sigue usando DsOC)→RepositorioCodeas.CargarASMaestras. **REGLA (usuario): repo coherente con la BD del dato, no con el DataSet** — al migrar, comprobar el dominio real de cada tabla (TR_*=Transporte, COD_/SAC/covibase=Codeas, FAC_*=FAC, IN_*=Inventario) y usar el repo correcto; un form puede repartir métodos entre repos. ✅ IA\frmInformacionAsoc (VERIFICADO ya-migrado), ✅ Visores\FrmVisorPrestamos (09f803d, el grande), ✅ SAC\Visores\frmVisorOrdenesSAC (d80d618)→RepositorioCodeas.BuscarOrdenesSac (SP SAC_BuscarAsoc; grid 10 cols en código). ✅ **`VarGlobales.consultas` ELIMINADO (d80d618)**: los 2 últimos usos (`PR_VerificarCtaCont` en INV\frmVisorInventarioSync y OC\VisOCCodeas) → `_repoCodeas.VerificarCuentaContable`. **ESTADO DsCodeasAdiggm: ya NO lo usa ningún form salvo Reportes\RptMaestro (dominio DsTransporteAdiggm → fase Transporte) y PRESUPUESTO\frmPresupuestoSem (DIFERIDO).** El `.xsd` se borrará al cerrar esos dos (no antes). En la práctica, achicar DsCodeasAdiggm está HECHO salvo lo diferido/Transporte. **Patrón maestro-combo + grid hijo por DataRelation** (frmSubMenu/FrmAsigTpFacTpVeh): DataSet en memoria con 2 tablas + `Relations.Add(nombre, padre.Col, hijo.Col, createConstraints:false)`; combo→`padreBindingSource`, grid→`fkBindingSource` (DataSource=padreBindingSource, DataMember=nombreRelación, asignado en código); guardar persiste la tabla hija (hereda el FK). **Gotcha §8**: para retirar DsCodeasAdiggm por completo hay que reemplazar también `VarGlobales.consultas` (SP globales de Codeas). UN form por turno; **análisis de errores de lógica (§0)**, GRAVES se reparan; build verde + commit + actualizar §6/§9/§12/§13. **Gotchas §11 (grid sin DataSource de diseño) y bindings de diseño huérfanos en TODO Designer.** Patrones probados (reutilizar): Opción A (mant. grid editable), columna combo en grid, SP→grid+combo, SP con OUTPUT (`DynamicParameters` InputOutput), scalar SP (`Escalar<T>`).
PENDIENTE A EVALUAR (§14.7): transaccionalizar el flujo Generar de FrmSolCred (InsertarAsociado→InsertarSolicitud→TablaAmortizacion→EstadoFinanciero×N van sin transacción, igual que el original; los SPs parecen tener semántica upsert propia — decidir con el usuario y la BD de prueba).

## 13.c TAREA PARALELA (feature nueva del usuario) — Actualizar tarifas por % (FrmActualizarTarifas)
Nuevo form en Mant para subir/bajar tarifas aplicando un %. 3 procesos **independientes**: (a) base `TR_AsigRutaTipoVeh` (solo filtro TipoVeh; Cliente/Clase se deshabilitan), (b) asignadas `TR_TarifaRutas` (Cliente+Clase+TipoVeh), (c) boletas `TR_Viajes` (rango de 2 fechas libres; excluye anuladas y cierres cerrados `TR_CierreClientes.Cerrado=1/Anulado=0`; recalcula Subtotal/ISV/Total preservando la tasa ISV=ISV/Subtotal). Filtros estilo FrmAsigTarifas (Cliente/TipoVeh/ClaseTrab combos + texto Ruta opcional). % global para todo el conjunto + **previa editable por fila** (4 decimales, redondear). Aplicar = transacción todo-o-nada + auditoría. **Restricciones del usuario**: NADA de SPs ni cambios directos a BD (consultas en C#/Dapper); tablas nuevas se entregan como script SQL para que el usuario las ejecute. **Auditoría GENÉRICA del sistema**: tabla `ADI_Auditoria` (Usuario, Modulo, Accion, Tabla, IdRegistro, ValorAnterior, ValorNuevo, Detalle) — reutilizable en TODO el proyecto; vive en BD TransporteAdiggm por ahora (script entregado, usuario la crea). El % y los filtros van en Detalle. ✅ **Capa de datos lista (a76725e, auditoría genérica 2680a95)**: POCOs `FilaTarifa`/`FilaViaje` (CapaModelo) + métodos en `RepositorioTransporte` (PreviewTarifasBase/Asignadas, PreviewViajes, AplicarTarifasBase/Asignadas, AplicarViajes; firmas `(filas, usuario, detalle)`; helper ParamsAuditoria). ✅ **FORMULARIO LISTO (757b133; rediseño a pestañas 55e50e6)**: `Mant\FrmActualizarTarifas` (FrmPrincipal): **TabControl** con 3 pestañas (Tarifa base / asignada / Boletas), cada una con SOLO sus filtros (Base=tipo veh.; Asignada=cliente+tipo veh.+clase; Boletas=igual+Desde/Hasta). Sin filtro de Ruta. Combos por pestaña (DataTable propio c/u). `nudPorcentaje` con recálculo en vivo, `dgvPrevia` (columnas en código, solo "Tarifa nueva" editable, recálculo Subtotal/ISV/Total en boletas), Aplicar con confirmación → repo transaccional + auditoría. **OJO**: la ruta se compone `(Origen + ' - ' + Destino) AS Ruta` (TR_Rutas NO tiene columna Ruta — fue un bug corregido en 55e50e6). **PENDIENTE DEL USUARIO**: (1) ejecutar el script de `ADI_Auditoria`; (2) agregar la entrada de menú en el MDI; (3) validar en ejecución. (Tarea paralela a §13.b; FrmSolCred sigue pendiente del barrido §13.b.)

## 14. Revisión de lógica de ASIGNAR PERMISOS (auditoría 2026-06-10; fixes 1-5 APLICADOS en d7bf391 y dd8a95b; queda el 6 como deuda de diseño)
1. **GRAVE — guardado sin transacción** (`frmAsigPermisos.btnGuardar`): hace `DELETE FROM Permiso WHERE IdUsuario=...` y luego inserta fila por fila con `usp_ActualizarPermisos`; si algo falla a medio camino el usuario queda con permisos parciales o SIN permisos. Fix: envolver DELETE+INSERTs en UNA transacción (repo con `CrearConexion()`+BeginTransaction) y rollback ante error.
2. **GRAVE — éxito mentiroso + catch por fila**: el catch interno del foreach traga errores y continúa, y al final SIEMPRE muestra "Permisos actualizados exitosamente". Fix: con la transacción del punto 1, un solo try: o todo o nada, y mensaje acorde.
3. **RIESGO — `Convert.ToBoolean(DBNull)`**: la columna `Habilitado` de usp_CargarPermisos es nullable (xsd L370 minOccurs=0); si el SP devolviera NULL, `Convert.ToBoolean(row.Cells["habilitado"].Value)` lanza InvalidCastException DESPUÉS del DELETE (pérdida total). Fix: tratar DBNull como false.
4. **MENOR — botón marcar/desmarcar desincronizado**: `selectAllOff` y el ícono no se resetean al cambiar usuario/recargar (`cargarDgv`); el primer click tras cambiar de usuario puede desmarcar en vez de marcar. Fix: resetear flag+ícono en cargarDgv().
5. **LATENTE — parseo XML mal anidado** (`CD_Usuario.ObtenerPermisos` L79-80): los nietos se leen de `menu...Elements("SubMenu").Elements("DetalleSubMenuNieto")` (la UNIÓN de todos los submenús hermanos) en vez de `submenu.Elements("DetalleSubMenuNieto")`. HOY no altera la visibilidad del MDI (enciende por nombre con búsqueda global, la unión repetida da lo mismo), pero cualquier consumo futuro por submenú saldrá mal. Fix de 2 líneas cuando se toque CD_Usuario; NO urgente.
6. **FRAGILIDAD (diseño, no tocar por ahora)**: el MDI aplica permisos buscando ítems por TEXTO del menú (`busca(item, nombre)`); renombrar un ítem en BD o en el Designer rompe el permiso en silencio. El catch de ObtenerPermisos también devuelve lista vacía ante CUALQUIER error de BD (usuario "sin permisos" sin aviso).
7. **DEUDA — FrmSolCred.Generar sin transacción** (decisión pendiente): la cadena InsertarAsociado→InsertarSolicitud→TablaAmortizacion→InsertarEstadoFinanciero×N replica el original sin transacción global. Los SPs reciben IdSolicitud (semántica upsert aparente), por lo que transaccionalizar a ciegas podría chocar con su lógica interna. Evaluar con el usuario contra la BD.
8. **Bugs reparados en FrmSolCred (0fc38a3)**: acumuladores T/P fuera del loop en modo editar (montos inflados); EXEC por concatenación con cadena legacy ×3; Clipboard.SetText con vacío.
9. **DEUDA — FAC_Factura.btnGuardar sin transacción** (migrado dad9c7d; decisión pendiente, análogo a §14.7): inserta la cabecera con `FAC_FacturaInsert` (devuelve IdFAC + @Mensaje) y luego, sólo si `@Mensaje=="SI"`, recorre el grid insertando cada línea con `FAC_FacturaDetInsert` SIN transacción global. Si una línea falla a media iteración, queda una factura con detalle parcial (total cabecera ≠ suma detalle). NO se transaccionalizó en la migración (fiel al original; el SP de cabecera reserva el correlativo de CAI y se desconoce su semántica interna). Evaluar con el usuario/BD si conviene envolver cabecera+detalles en `CrearConexion()`+BeginTransaction (pasando la transacción a cada SP). Migración fiel: comportamiento idéntico al DataSet original.
10. **GRAVE reparado (58339a8) — grid no editable al 1er clic de "Editar" tras §13.b** (hallado por el usuario validando Seguridad 2026-06-17): efecto colateral de mover las columnas a código. Con las columnas creadas en `ConfigurarColumnas()` (tras `InitializeComponent`) con `ReadOnly=true`, el cascade de `DataGridView.ReadOnly = false` (en btnNuevo/btnEditar) **ya no reactivaba las columnas al primer clic**: el grid quedaba bloqueado hasta hacer Cancelar (pone el grid en `ReadOnly=true`) + Editar de nuevo (transición true→false REAL que sí dispara el cascade). Fix: helper **`Clases.GridColumnas.Edicion(dgv, habilitar)`** que fija `dgv.ReadOnly` Y el `ReadOnly` de cada columna a la vez (sin depender del cascade). **Refinamiento (d7962d6)**: al habilitar, el helper respeta las columnas enlazadas a un `DataColumn` de SOLO LECTURA (PK identity que `DataTable.Load` marca read-only, o columnas calculadas — gotcha §11) dejándolas en `ReadOnly=true`; ponerlas en false lanzaba `InvalidOperationException` ("...enlazada a un campo de solo lectura debe tener ReadOnly en True"). Detecta el DataTable del grid vía BindingSource/DataView (cubre binding por DataRelation). Aplicado en la ruta de habilitación de los 11 mantenimientos con columnas-en-código (frmTipoOp, frmBodegas, frmMenuSistema, frmSubMenu, frmDetSubMenu, FrmTipoFac, FrmAsigTpFacTpVeh, FAC_TipoMoneda, FAC_CAI, FAC_TipoFacturas, FAC_Productos). **Regla para lo que falta de §13.b (IA/CA, frmEditarAsistencia): usar SIEMPRE `GridColumnas.Edicion`, nunca `dgv.ReadOnly=false` directo.** (frmAsigPermisos NO aplica: no tiene toggle Editar, habilita `Columns["habilitado"].ReadOnly=false` directo.)
